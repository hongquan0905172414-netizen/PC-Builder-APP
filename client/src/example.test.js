// Example test — proves the testing tool (Vitest) works.
//
// Run all tests with:  npm test
//
// This is a placeholder. Delete it once there's real code to test.
// Real tests will check real logic — for example, the rules engine
// once it exists.

import { describe, it, expect } from "vitest";

describe("example", () => {
  it("math still works", () => {
    expect(1 + 1).toBe(2);
  });
});
