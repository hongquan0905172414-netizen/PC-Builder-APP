# components

Reusable React components go here — buttons, cards, layout pieces,
anything used in more than one place.

Nothing here yet. Build components as screens need them.

Loose guideline: one component per file, filename matches the
component name (e.g. `Button.jsx` exports `Button`).
