# pages

Full-screen views go here — the landing page, parts selection, the
build guide, etc. Each page is a React component.

Nothing here yet. The landing page will likely be the first one.

How pages connect (routing — which URL shows which page) hasn't been
set up yet. When the project needs more than one page, a routing
library like `react-router-dom` gets added. Not needed until then.
