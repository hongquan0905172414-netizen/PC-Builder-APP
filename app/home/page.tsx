/**
 * Home tab.
 *
 * Will contain:
 * - Active Build card (current step, % complete, quick resume)
 * - Create build entry point
 * - Quick start entry point (gaming / budget / etc.)
 */
export default function HomePage() {
  return <main>Home</main>;
}
