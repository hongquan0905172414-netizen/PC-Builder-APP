/**
 * Root page. This will eventually be the Home screen with the
 * Active Build card, Create build / Quick start entry points, and
 * the bottom nav bar.
 *
 * For now it's an empty placeholder.
 */
export default function Page() {
  return (
    <main>
      <h1>RIG</h1>
      <p>Skeleton in place. Nothing built yet.</p>
    </main>
  );
}
