/**
 * My Parts tab.
 *
 * Saved parts + compatibility view. Populated by the Assembly flow
 * ("Mark complete + progress saves to My Parts").
 */
export default function MyPartsPage() {
  return <main>My Parts</main>;
}
