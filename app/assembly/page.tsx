/**
 * Assembly tab.
 *
 * Step-by-step build flow:
 * 1. Confirm parts first (rules engine runs here)
 * 2. Step view + media
 * 3. Mark complete + progress (saves to My Parts)
 *
 * Leads into First Boot Checklist (BIOS, drivers, RGB, OS install).
 */
export default function AssemblyPage() {
  return <main>Assembly</main>;
}
