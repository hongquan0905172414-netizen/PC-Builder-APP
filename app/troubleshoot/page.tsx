/**
 * Troubleshoot tab.
 *
 * Diagnosis paths:
 * - During build
 * - After first boot
 * - Escalate to community
 *
 * Note: during-build help may also be reachable contextually from
 * inside Assembly steps. To be designed.
 */
export default function TroubleshootPage() {
  return <main>Troubleshoot</main>;
}
